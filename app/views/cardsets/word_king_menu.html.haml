

.cm-container  
  .mypage-content.main-content
    %h1.page-title 受けるテストを選んでください
    .p-3.table-responsive
      %table.table.table-hover
        %tbody.thead-default
          - @cardsets.each do |cardset|
            %tr
              %td.cardset-item
                = link_to cardset.name, word_king_teacher_cardset_path(current_student, cardset)
                .learning-icon
                  %a
                    %span 学習中 
                    %i{class: "fas fa-pencil-alt"}

